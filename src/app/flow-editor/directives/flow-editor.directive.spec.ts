import { FlowEditorDirective } from './flow-editor.directive';

describe('FlowEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new FlowEditorDirective();
    expect(directive).toBeTruthy();
  });
});
