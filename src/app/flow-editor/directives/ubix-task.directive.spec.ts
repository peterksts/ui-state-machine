import { UbixTaskDirective } from './ubix-task.directive';

describe('UbixTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new UbixTaskDirective();
    expect(directive).toBeTruthy();
  });
});
